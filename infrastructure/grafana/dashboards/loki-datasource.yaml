apiVersion: v1
kind: Secret
metadata:
  namespace: monitoring
  name: grafana-datasources
  labels:
    grafana_datasource: 'true'
stringData:
  prometheusloki.yaml: |-
    apiVersion: 1
      datasources:
        - name: Prometheus
          type: Prometheus
          url: http://prometheus.monitoring.svc.cluster.local
          reloadURL: http://prometheus.monitoring.svc.cluster.local/api/admin/provisioning/datasources/reload
          access: proxy
          isDefault: true
          editable: false