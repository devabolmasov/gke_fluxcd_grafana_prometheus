apiVersion: v1
kind: ConfigMap
metadata:
  namespace: monitoring
  name: prometheus-loki-datasource-sidecar
  labels:
    grafana_datasource: "1"
data:
  prom-loki.yaml: |-
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        editable: false
        url: http://test.monitoring.svc.cluster.local
        access: proxy
        isDefault: false
    